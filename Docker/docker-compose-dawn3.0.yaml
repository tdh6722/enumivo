version: "3"

services:
  nodeosd:
    image: enumivo/enumivo:dawn3x
    command: /opt/enumivo/bin/nodeosd.sh --data-dir /opt/enumivo/bin/data-dir
    hostname: nodeosd
    ports:
      - 8888:8888
      - 9876:9876
    expose:
      - "8888"
    volumes:
      - nodeos-data-volume:/opt/enumivo/bin/data-dir

  keosd:
    image: enumivo/enumivo:dawn3x
    command: /opt/enumivo/bin/keosd --wallet-dir /opt/enumivo/bin/data-dir
    hostname: keosd
    links:
      - nodeosd
    volumes:
      - keosd-data-volume:/opt/enumivo/bin/data-dir

volumes:
 nodeos-data-volume:
   external: true
 keosd-data-volume:
   external: true